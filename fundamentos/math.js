// Nosso querido Math é um objeto que usamos para fazer cálculos mais complicados.

// Segue um exemplo bem simples.
const raio = 5.6;
const area = Math.PI * Math.pow (raio, 2); // PI é PI, pow é potência.

console.log(area); // Resultado da brincadeira.
console.log(typeof Math); // Math é um objeto.