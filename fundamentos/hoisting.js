// Exemplo de hoisting na veriavel var do JavaScript
console.log('a =', a); // a = undefined (mesmo não crida, passa como se existisse)
var a = 2; // Definindo valor.
console.log('a =', a);

/*
    // Exemplo de hoisting em JavaScript
    console.log('b =', b); // Não há hosting com o let.
    let b = 2; // Definindo valor.

    console.log('b =', b);
*/