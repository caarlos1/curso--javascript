let valorQualquer; //  Não inicializamos a variavel, apenas declaramos.
console.log(valorQualquer); // Resultado: undefined

valorQualquer = null; // Inicializamos com a auxencia de valor na memória.
console.log(valorQualquer);

/* 
    Não definida (undefined) é != de Nulo (null).
    Cuidado!!!!!
*/